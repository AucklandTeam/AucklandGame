
const verApi = '/api/v1';

export const ApiLocation = {
    BASE: verApi,
    USER: {
        SUNC: `${verApi}/user-sync`
    },
    FORUM: `${verApi}/forum`,
    TOPICS: `${verApi}/topic/all`,
    TOPIC: `${verApi}/topic`,
}