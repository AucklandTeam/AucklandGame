import React from 'react'
import ArrowIcon from '../Icons/ArrowIcon'
import ButtonIcon from '../Icons/ButtonIcon'

const ConfigArrowButton = ({ deg = 0, margin = 0, marginLeft = 0 }) => (
	<ButtonIcon deg={deg} margin={margin} marginLeft={marginLeft} />
)

export default ConfigArrowButton
